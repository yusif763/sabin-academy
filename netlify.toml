[build]
  command = "prisma migrate deploy && npm run db:seed && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[context.production.environment]
  NEXTAUTH_URL = "https://sabinaacademy.com"
  NEXT_PUBLIC_APP_URL = "https://sabinaacademy.com"

[context.deploy-preview.environment]
  NEXTAUTH_URL = "${DEPLOY_PRIME_URL}"
  NEXT_PUBLIC_APP_URL = "${DEPLOY_PRIME_URL}"

[context.branch-deploy.environment]
  NEXTAUTH_URL = "${DEPLOY_PRIME_URL}"
  NEXT_PUBLIC_APP_URL = "${DEPLOY_PRIME_URL}"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200

# Static fayllar
[[redirects]]
  from = "/_next/*"
  to = "/_next/:splat"
  status = 200

# Dil-spesifik route-lar - bunlar qalır
[[redirects]]
  from = "/az/*"
  to = "/az/:splat"
  status = 200

[[redirects]]
  from = "/ru/*"
  to = "/ru/:splat"
  status = 200

[[redirects]]
  from = "/en/*"
  to = "/en/:splat"
  status = 200

# ROOT - /en-ə redirect
[[redirects]]
  from = "/"
  to = "/en"
  status = 302

# Qalanlar - /en-ə redirect (ən son qaydadır!)
[[redirects]]
  from = "/*"
  to = "/en/:splat"
  status = 302

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer-when-downgrade"